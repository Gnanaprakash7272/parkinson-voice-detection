"""Make this directory a Python package so imports like
`from parkinson_voice_detection.extract_features import extract_features`
work from project root and so language servers (Pylance) can resolve it.
"""

# package marker
